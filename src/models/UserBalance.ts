export interface UserBalance {
    id: string;
    account_id: string;
    market_id: string;
    outcome: number;
    shares_balance: string;
    tokens_to_spend: string;
    tokens_spent: string;
    avg_price_per_share: string;
}
